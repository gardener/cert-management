helper: (( merge )) 

settings:
  repokey: ""
  pipeline: cert-management
  goVersion: 1.12.8
  branchsets:
    main: (( .helper.main_branch("master") ))
    patch: (( .helper.patch_branch("patch") ))
