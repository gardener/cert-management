helper: (( merge )) 

settings:
  repokey: ""
  pipeline: cert-management
  goVersion: 1.13.9
  branchsets:
    main: (( .helper.main_patch_branch("master") ))
    patch: (( .helper.patch_branch("patch") ))
